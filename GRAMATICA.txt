#Variáveis
S GERAL MAIN BLOCO DECLARACAO COMANDO IF ATRIBUICAO WHILE FOR DELCARACAO TIPO IDENTIFICADOR EXP_COND ELSE EXP_AR MUL MENOSP NUM
EXP_BOOL OP_LOGICO EXP_NEGACAO EXP_RELACIONAL OP_RELACIONAL
# Terminais
int float double string char boolean while for if else == =! > < >= <= numero literal = { } [ ] ( ) + - * / ^ v ! true false
#Regras

S -> GERAL

GERAL  -> MAIN

MAIN -> { BLOCO } | { }

BLOCO -> DECLARACAO BLOCO
BLOCO -> BLOCO
BLOCO -> DECLARACAO
BLOCO -> BLOCO COMANDO
BLOCO -> COMANDO

COMANDO -> IF
COMANDO -> ATRIBUICAO
COMANDO -> WHILE
COMANDO -> FOR

DECLARACAO -> TIPO IDENTIFICADOR ;
TIPO -> int | float | double | string | char | bool
IDENTIFICADOR -> id | id = EXP_AR

IF -> if ( EXP_COND ) { BLOCO } ELSE
IF -> if ( EXP_COND ) { BLOCO }
IF -> if ( EXP_COND ) {  } ELSE
IF -> if ( EXP_COND ) {  }

ELSE -> else { BLOCO }
ELSE -> else { }

EXP_AR -> EXP_AR + MUL
EXP_AR -> EXP_AR - MUL
EXP_AR -> MUL

MUL -> MUL * MENOSP
MUL -> MUL / MENOSP
MUL -> MENOSP

MENOSP -> ( EXP_AR )
MENOSP -> - ( EXP_AR )
MENOSP -> - NUM
MENOSP -> NUM

NUM -> id | numero

ATRIBUICAO -> id = EXP_AR
ATRIBUICAO -> id = numero
ATRIBUICAO -> id = literal
ATRIBUICAO -> id = true
ATRIBUICAO -> id = false

EXP_COND -> EXP_BOOL OP_LOGICO EXP_BOOL
EXP_COND -> EXP_BOOL OP_LOGICO EXP_RELACIONAL
EXP_COND -> EXP_RELACIONAL OP_LOGICO EXP_BOOL
EXP_COND -> EXP_RELACIONAL OP_RELACIONAL EXP_BOOL
EXP_COND -> EXP_BOOL
EXP_COND -> EXP_RELACIONAL

EXP_BOOL -> EXP_BOOL OP_LOGICO EXP_BOOL
EXP_BOOL -> EXP_BOOL
EXP_BOOL -> id | true | false | EXP_NEGACAO

OP_LOGICO -> ^ | v
EXP_NEGACAO -> !EXP_BOOL | !id

EXP_RELACIONAL -> EXP_AR OP_RELACIONAL EXP_AR

OP_RELACIONAL -> == | =! | < | > | <= | >=

WHILE -> ( EXP_COND ) { BLOCO }

FOR -> ( ATRIBUICAO ; EXP_COND ; ATRIBUICAO ) { BLOCO }

